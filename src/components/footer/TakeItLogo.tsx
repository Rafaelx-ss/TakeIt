import React from 'react';

const TakeItLogo = () => {
    return (
        <div className="flex items-center">
            <span className="ml-2 text-2xl font-bold text-accent">Take It!</span>
        </div>
    );
};

export default TakeItLogo;
